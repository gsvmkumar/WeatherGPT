# WeatherGPT Mobile (React Native / Expo) — Environment Configuration Template
# Optional configuration. By default, the mobile app dynamically detects
# the host machine's IP address from the Metro bundler in Expo Go.

# Explicit Backend URL override (if not using automatic Metro host resolution)
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
